/**
 * Puerto de entrada para obtener la identidad autenticada sin acoplar
 * el caso de uso al proveedor de autenticación.
 * @typedef {Object} CurrentUserProvider
 * @property {() => Promise<string>} getCurrentUserId
 */

export {}
