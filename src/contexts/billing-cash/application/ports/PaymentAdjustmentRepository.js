/**
 * Puerto driven para ajustes administrativos de snapshots de pago.
 * La implementación debe actualizar pago, caja y auditoría en una única
 * transacción de infraestructura.
 *
 * @typedef {Object} PaymentAdjustmentRepository
 * @property {(adjustment: import('../../domain/entities/PaymentAdjustment.js').PaymentAdjustment) => Promise<import('../../domain/entities/PaymentAdjustment.js').PaymentAdjustment>} adjust
 */

export {}
